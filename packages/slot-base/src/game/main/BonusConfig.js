export default class BonusConfig {}
/**
 * 此Config 只供給 Bonus設定使用
 */
// 選擇的場次
BonusConfig.SELECT_STATE = 4;

// 選擇的位置
BonusConfig.SELECT_INDEX = 0;

// 遊戲Reel位置
BonusConfig.SELECT_REEL = -1;
BonusConfig.SELECT_ROW = -1;

// 是否可以離開Bonus Game
BonusConfig.IS_GAME_OUT = false;
// 設置觸發的Bonus Type   0 : LuckyDraw, 1 : BonusGame
BonusConfig.BONUS_STAGE_STATUS = 0;
